# Project Rules

- **Language Constraint:** All code and code comments must be written in English. Do not write Hungarian comments in the codebase.
- **Dependency Versioning:** All packages added to requirements.txt files must have explicit version pins (e.g., package==1.2.3). If unsure, install the package to verify the working version, and then pin that exact version in the requirements file.
